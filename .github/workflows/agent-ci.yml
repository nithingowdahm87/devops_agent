# -*- coding: utf-8 -*-
name: DevOps Agent V12.0 Core CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  validate:
    name: "Locked Compiler Validation"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install flake8 pytest mypy pyyaml requests python-dotenv pydantic
          
      - name: Lint & Static Analysis
        run: |
          flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
          mypy src --ignore-missing-imports
          
      - name: Core Unit Tests
        run: |
          pytest tests/test_ooda_healer.py
          # pytest # Run all when fully stable
