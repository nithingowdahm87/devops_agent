# ─── DevOps Agent — Environment Variables ───────────────────────────
# Copy this file to .env and fill in your API keys.
# For production, use AWS Secrets Manager or HashiCorp Vault instead.
# ─────────────────────────────────────────────────────────────────────

# LLM Provider Keys (required for live mode, omit for mock mode)
GOOGLE_API_KEY=your_google_api_key_here
GROQ_API_KEY=your_groq_api_key_here
NVIDIA_API_KEY=your_nvidia_api_key_here

# ─── Optional: AWS Secrets Manager ──────────────────────────────────
# If using AWS SM, set these and store keys under the secret name below.
# AWS_REGION=ap-south-1
# DEVOPS_AGENT_SECRET_NAME=devops-agent/llm-keys

# ─── Optional: HashiCorp Vault ──────────────────────────────────────
# VAULT_ADDR=https://vault.example.com
# VAULT_TOKEN=your_vault_token
# VAULT_SECRET_PATH=secret/data/devops-agent/llm-keys

# ─── Optional: GitOps (PR mode) ────────────────────────────────────
# If set, approved artifacts are published as PRs instead of local writes.
# GITHUB_TOKEN=your_github_personal_access_token
# GITHUB_REPO=owner/repo
# GITHUB_BASE_BRANCH=main

# ─── Optional: Logging ─────────────────────────────────────────────
# LOG_JSON=true  # Enable JSON structured logging (production)
