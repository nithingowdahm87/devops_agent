apiVersion: v1
kind: Service
metadata:
  name: devops-agent
  namespace: devops-agent
  labels:
    app: devops-agent
    version: "1.0.0"
spec:
  type: ClusterIP
  selector:
    app: devops-agent
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
