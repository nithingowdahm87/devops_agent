apiVersion: v1
kind: ConfigMap
metadata:
  name: devops-agent-config
  namespace: devops-agent
  labels:
    app: devops-agent
    version: "1.0.0"
data:
  APP_VERSION: "1.0.0"
  ENVIRONMENT: "production"
  SERVER_HOST: "0.0.0.0"
  SERVER_PORT: "8000"
  LOG_JSON: "true"
  CORS_ALLOWED_ORIGINS: ""
