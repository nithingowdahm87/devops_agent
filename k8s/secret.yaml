apiVersion: v1
kind: Secret
metadata:
  name: devops-agent-secrets
  namespace: devops-agent
  labels:
    app: devops-agent
    version: "1.0.0"
type: Opaque
stringData:
  DATABASE_URL: "postgresql://user:pass@db:5432/devops_agent"
  OPENAI_API_KEY: "REPLACE_ME"
  GITHUB_TOKEN: "REPLACE_ME"
