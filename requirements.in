# ─── DevOps Agent — Core Dependencies ───────────────────────────
# Use pip-compile to generate requirements.lock:
#   pip-compile requirements.in -o requirements.lock
# ─────────────────────────────────────────────────────────────────

# LangChain (Gemini integration)
langchain
langgraph
langchain-google-genai

requests

# YAML parsing
pyyaml

# Schema validation
pydantic>=2.0

# Retries
tenacity

# ─── Optional: Production backends (uncomment as needed) ────────
# boto3              # AWS Secrets Manager
# hvac               # HashiCorp Vault

# ─── Dev / Test ─────────────────────────────────────────────────
pytest>=7.0
pytest-cov
